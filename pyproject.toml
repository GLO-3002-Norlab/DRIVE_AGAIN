[project]
name = "DRIVE_AGAIN"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.19.5",
    "black",
    "pytest",
    "numpy",
]

[tool.black]
line-length = 120
